{
  "version": 2,
  "buildCommand": "npm run build -w packages/shared",
  "builds": [
    { "src": "packages/server/api/index.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "packages/server/api/index.ts" }
  ]
}
